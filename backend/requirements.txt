fastapi==0.136.0
starlette==1.3.1
uvicorn==0.30.0
websockets==15.0.1
pydantic==2.9.2
python-multipart==0.0.32
aiofiles==24.1.0
gunicorn==22.0.0
httpx==0.28.1
httpx2==2.9.1  # Starlette 1.x TestClient transport
anyio==4.14.2
PyJWT[crypto]==2.13.0  # Cryptographic Apple/Google ID-token verification
cryptography==46.0.5  # Direct dependency for Amazon SNS RSA signature verification
bcrypt==5.0.0  # Secure password hashing (P0 security fix — replaces SHA-256)
# redis  # Uncomment for production with Redis backend
# python-jose[cryptography]  # For future JWT authentication
python-dotenv==1.2.2  # For environment variable management
boto3==1.42.89  # Amazon Bedrock integration for AI services
alembic==1.18.5
qrcode==8.2  # Server-generated SVG QR codes for TOTP enrollment

# Keep packaging tools present in the runtime base image on patched versions.
pip==26.2
setuptools==83.0.0
